# Use R to extract the square root of 85
sqrt(85)

# Compute the following equation in R
15/(10^2)

# Compute the following equation in R
(215+log(100))/(exp(100))

# Use R to compute the natural logarithm of 24
log(24)
?log()
